#include<iostream>
using namespace std;
int main()
{
    int a=4,b=5;
    // Arethmatic operator
    cout<<"\n Examples of Arethmatic opertors";
    cout<<"\n The values of the a+b :- "<<(a+b);
    cout<<"\n The values of the a-b :- "<<(a-b);
    cout<<"\n The values of the a*b :- "<<(a*b);
    cout<<"\n The values of the a/b :- "<<(a/b);
    cout<<"\n The values of the a%b :- "<<(a%b);
    cout<<"\n The values of the a++ :- "<<(a++);
    cout<<"\n The values of the b++ :- "<<(b++);
    cout<<"\n The values of the ++b :- "<<(++b);
    cout<<"\n The values of the --a :- "<<(--a);
    cout<<"\n The values of the a-- :- "<<(a--);
    cout<<"\n The values of the b-- :- "<<(b--);
    cout<<"\n The values of the --b :- "<<(--b);
    cout<<"\n The values of the --a :- "<<(--a);
      
    // comprison operator
    cout<<"\n Examples of comprison opertors";
    cout<<"\n The values of the a==b :- "<<(a==b);
    cout<<"\n The values of the a!=b :- "<<(a!=b);
    cout<<"\n The values of the a>=b :- "<<(a>=b);
    cout<<"\n The values of the a<=b :- "<<(a<=b);
    cout<<"\n The values of the a>b :- "<<(a>b);
    cout<<"\n The values of the a<b :- "<<(a<b);
      
    // logical operator
    cout<<"\n Examples of logical  opertors";
    cout<<"\n The values of the ((a==b) && (a<b)) :- "<<((a==b) && (a<b));
    cout<<"\n The values of the ((a==b) || (a<b)) :- "<<((a==b) || (a<b));
    cout<<"\n The values of the (!(a==b)) :- "<<(!(a==b));
    return 0;
      

}